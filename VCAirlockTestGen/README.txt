1. export the sub-table from google sheet as csv
2. open the csv using Numbers the export it to also csv file
3. use vim to view the final csv file and copy and paste it to the code in the ViewController.swift as "toBeParsed"
